import React from 'react'

const Paginate = (arr, page) => {
  console.log(arr)
}
const Pagination = ({arr}) => {
    arr.map(x => {
        
    })
  return (
    <div>Pagination</div>
  )
}

export default Pagination